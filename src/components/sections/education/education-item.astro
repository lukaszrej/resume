---
import type { Education } from "@/lib/types";
import EducationPositionItem from "./position-item.astro";

interface Props {
  education: Education;
}

const { education } = Astro.props;
---

<div class="space-y-4 p-4 not-last:border-b">
  <div class="flex items-center space-x-4 px-2">
    <span class="size-2 rounded-full bg-foreground/60"></span>

    <h3 class="text-xl font-black">
      {education.institution}
    </h3>
  </div>

  <div class="relative space-y-4 pl-1">
    {
      education.positions.map(position => {
        return <EducationPositionItem position={position} />;
      })
    }
  </div>
</div>
